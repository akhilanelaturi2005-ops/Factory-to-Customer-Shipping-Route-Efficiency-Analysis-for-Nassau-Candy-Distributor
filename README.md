
Nassau Candy Distributor – Shipping Route Efficiency Analysis

How to Run:
1. Install dependencies:
   pip install streamlit pandas numpy

2. Run the app:
   streamlit run app.py

This dashboard demonstrates route efficiency, ship mode comparison,
and filtered analytics. Replace sample data with full dataset.
